{
    "scripts": {
        "build": "npx tsc",
        "start": "npx tsc && cp -r assets/ target/ && node target/index.js"
    },
    "devDependencies": {
        "@types/node": "^18.15.3",
        "typescript": "^4.9.5"
    },
    "dependencies": {
        "prompt-sync": "^4.2.0"
    }
}
